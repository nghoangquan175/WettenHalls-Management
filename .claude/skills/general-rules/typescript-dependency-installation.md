When suggesting installation commands:
- include runtime package
- include @types package if needed
- use -D for development-only packages
- mention whether the package already includes built-in types